- id: Preflight Checks
  desc: "Run a preflight check to ensure the environment is ready for installation"
  kind: python3
  file: "/data/tasks/basekit/preflight.py"

- id: Bootstrap Mgmt KVM
  desc: "Configure/Install services on the mgmt-kvm server"
  kind: ansible
  file: "/data/tasks/basekit/bootstrap-mgmt-kvm.yml"

- id: Deploy VMs
  desc: "Deploy the VMs for the base-kit profile"
  kind: ansible
  file: "/data/tasks/basekit/deploy-vms.yml"

# OPNSense is already configured from the saved config directory.
# Will need to do this if things change
#- id: Configure OPNsense
#  desc: "Configure the OPNsense VM"
#  kind: ansible
#  file: "/data/tasks/basekit-configure-opnsense.yml"

- id: Deploy OPNsense
  desc: "Deploy the OPNsense VM"
  kind: python3
  file: "/data/tasks/basekit/deploy-opnsense.py"